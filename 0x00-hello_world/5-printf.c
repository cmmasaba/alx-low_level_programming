#include<stdio.h>
/**
 * main - prints the message and adds new line
 * Return: 0 if no error is encountered
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
